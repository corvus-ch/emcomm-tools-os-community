Pre select answers for dialogs
Index: emcomm-tools-os-community/source/scripts/download-et-maps.sh
===================================================================
--- emcomm-tools-os-community.orig/source/scripts/download-et-maps.sh
+++ emcomm-tools-os-community/source/scripts/download-et-maps.sh
@@ -23,11 +23,16 @@ FILES=(
   ["world"]="osm-world-zoom0to7-20251121.mbtiles"
 )
 
-# Show dialog
-SELECTED_COUNTRY=$(dialog --clear --menu "Select a country" 10 40 5 "${OPTIONS[@]}" 2>&1 >/dev/tty)
-EXIT_STATUS=$?
+if [ -z ${ET_MAP_SELECTION+x} ]; then
+  # Show dialog
+  SELECTED_COUNTRY=$(dialog --clear --menu "Select a country" 10 40 5 "${OPTIONS[@]}" 2>&1 >/dev/tty)
+  EXIT_STATUS=$?
 
-tput sgr0 && clear
+  tput sgr0 && clear
+else
+  SELECTED_COUNTRY=$ET_MAP_SELECTION
+  EXIT_STATUS=0
+fi
 
 if [[ $EXIT_STATUS -eq 0 ]]; then
   DOWNLOAD_FILE="${FILES[$SELECTED_COUNTRY]}"
Index: emcomm-tools-os-community/source/scripts/download-osm-maps.sh
===================================================================
--- emcomm-tools-os-community.orig/source/scripts/download-osm-maps.sh
+++ emcomm-tools-os-community/source/scripts/download-osm-maps.sh
@@ -12,37 +12,42 @@ PBF_MAP_DIR=/etc/skel/my-maps
 
 [ ! -e ${PBF_MAP_DIR} ] && mkdir -v ${PBF_MAP_DIR}
 
-if [ ! -e ${HTML} ]; then
-  et-log "Downloading list of maps files for the US..."
-  curl -s -L -f -o ${HTML} ${URL}
-
-  if [ $? -ne 0 ]; then
-    et-log "Can't download list map data from ${URL}. Exiting..."
-    exit 1
+if [[ -z ${ET_OSM_URL+x} ]]; then
+  if [ ! -e ${HTML} ]; then
+    et-log "Downloading list of maps files for the US..."
+    curl -s -L -f -o ${HTML} ${URL}
+ 
+    if [ $? -ne 0 ]; then
+      et-log "Can't download list map data from ${URL}. Exiting..."
+      exit 1
+    fi
+  fi
+ 
+  et-log "Extracting state-level .pbf file list..."
+ 
+  options=()
+  while IFS= read -r state_html; do
+      state_file=$(echo $state_html | sed -n 's|.*href="\([^"]*\.osm\.pbf\)".*|\1|p')
+      state_url="https://download.geofabrik.de/north-america/${state_file}"
+ 
+      # Extract a display friendly name for the state
+      state_name=$(echo $state_file | sed 's|us/||' | sed 's|-latest.osm.pbf||')
+ 
+      options+=("$state_name" "$state_url")
+  done < <(grep "[.]pbf" page.html | grep 'href="us/' | sort)
+ 
+  SELECTED_STATE=$(dialog --clear --menu "Select a map file:" 20 80 10 "${options[@]}" 3>&1 1>&2 2>&3)
+  if [ $? -eq 0 ]; then
+    download_url="https://download.geofabrik.de/north-america/us/${SELECTED_STATE}-latest.osm.pbf"
   fi
-fi
-
-et-log "Extracting state-level .pbf file list..."
-
-options=()
-while IFS= read -r state_html; do
-    state_file=$(echo $state_html | sed -n 's|.*href="\([^"]*\.osm\.pbf\)".*|\1|p')
-    state_url="https://download.geofabrik.de/north-america/${state_file}"
-
-    # Extract a display friendly name for the state
-    state_name=$(echo $state_file | sed 's|us/||' | sed 's|-latest.osm.pbf||')
-
-    options+=("$state_name" "$state_url")
-done < <(grep "[.]pbf" page.html | grep 'href="us/' | sort)
-
-SELECTED_STATE=$(dialog --clear --menu "Select a map file:" 20 80 10 "${options[@]}" 3>&1 1>&2 2>&3)
-exit_status=$?
 
-tput sgr 0 && clear
+  tput sgr 0 && clear
+else
+  download_url=$ET_OSM_URL
+fi
 
-if [ $exit_status -eq 0 ]; then
-  download_file="${SELECTED_STATE}-latest.osm.pbf"
-  download_url="https://download.geofabrik.de/north-america/us/${download_file}"
+if [ ! -z download_url ]; then
+  download_file="$(basename $download_url)"
 
   if [ -e ${download_file} ]; then
     et-log "${download_file} already exists. Skipping download."
